name: Pull Request Badge
description: Adds badges to Pull Request based on configuration
inputs:
  repo-token:
    description: "The GitHub token to use for authentication"
    required: true
runs:
  using: "node16"
  main: "./.github/workflows/pull_request_badge.js"
